NEXTAUTH_SECRET=replace_with_strong_random_string
NEXTAUTH_URL=http://localhost:3000

# SQLite (dev) database for Prisma
DATABASE_URL="postgresql://ishaqbello@localhost:5432/test_db"

# Example GitHub OAuth (optional) https://github.com/settings/developers
GITHUB_ID=
GITHUB_SECRET=

# Payments (Stripe) – DO NOT COMMIT REAL KEYS
# Use real keys locally only in your untracked .env file. Placeholders below intentionally avoid real key prefixes.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=<your_publishable_key>
STRIPE_SECRET_KEY=<your_secret_key>
STRIPE_WEBHOOK_SECRET=<your_webhook_secret>

# Email (provider optional; fallback logs to console if unset)
EMAIL_FROM="Store <no-reply@example.com>"
RESEND_API_KEY=